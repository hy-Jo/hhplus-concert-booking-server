# Kafka Configuration
KAFKA_BROKERS=localhost:9092,localhost:9093,localhost:9094

# Database Configuration (기존 설정 유지)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=rootpassword
DB_DATABASE=concert_reservation

# Redis Configuration (기존 설정 유지)
REDIS_HOST=localhost
REDIS_PORT=6379
